clangd doesn't support single-module-compilation (a.k.a unity build)
https://github.com/clangd/clangd/issues/45

Requirements (Windows):
- FASTBuild
- Visual Studio
- DXC (with SPIR-V CodeGen enabled)

Requirements (MacOS):
- FASTBuild
- clang
